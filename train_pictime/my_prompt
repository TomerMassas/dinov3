TRIALS:

1. play with the n_blocks to unfreeze
2.lr_backbone sweep


2. use head or not use head?
3. tau sweep
4. train on bigger backbone (vitB16)
5. train on the whole dataset, and take into account that big gallerie might have many identities so we need to think of the p_value carefully
6. bigger pk batches
7. try arcface. we can also try arcface on weights that first trained with supcon, so its a second stage finetune
8. mask the faces before i train, so the features are based on body parts and not faces.



1. the freeze_last_layer_iters: param means, that after 200 iters we unfreeze?
2. add to the a100 config, all the interesting params (although it is duplicated) to it is easier to see and change, in the base config it is soo many it is easy to miss eyeballing
